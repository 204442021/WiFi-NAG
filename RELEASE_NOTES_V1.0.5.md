# WIFI-NAG V1.0.5

本版本新增通过 BLE 刹车状态驱动的障碍物换挡测试功能，并将固件内部版本统一更新为 `V1.0.5`。

## 更新内容

- BLE 功能独立为 NAG 下方的大卡片，并显示连接、协议、数据新鲜度与诊断状态。
- 在 BLE 卡片下方新增“障碍物换挡”大卡片，D/R 挡换挡开关首次默认开启。
- BLE 构建使用的固定过滤器新增标准帧 `0x118`；未编译或未初始化 BLE 功能的基础 NAG 构建仍只过滤 `0x370`，运行期不热切换过滤器。
- T2CAN 双源刹车确认有效且踩下、车速确认静止、真实 `0x118` 为 D/R 挡时，以每一帧实时原车数据生成虚拟 P 挡帧。
- 虚拟 P 最长维持 500 ms，随后锁存停止；收到明确释放尾帧后解除锁存并等待下一轮。
- 增加 BLE 会话、报文序列、数据新鲜度、发送失败与互斥条件的安全保护和诊断计数。

## 0x118 修改规则

- 只处理 8 字节标准数据帧，保持原帧 Byte1、Byte3、Byte4、Byte5、Byte7 不变。
- Byte2 修改为 `0x32`（P 挡），Byte6 修改为 `0x00`。
- Byte0 按 `Byte1..Byte7` 之和再加 `0x19` 重新计算低 8 位校验。
- 不缓存重放历史帧，不固定补发；只有触发后收到的新鲜真实 `0x118` 才能生成对应虚拟 P 帧。

## 安全说明

该功能目前仅供台架或封闭场地验证，不能作为道路安全功能使用。虚拟 P 与原车 `0x118` 在同一 CAN 总线上共存；出现异常时应立即关闭功能或移除设备。

本地提交不包含固件编译产物，也不创建或修改远端发布。
