# WIFI-NAG V1.0.2

本版本主要提升 OTA 与设备重启过程中的车辆通信稳定性。

## 更新内容

- 修复 OTA 完成并自动重启时，车辆偶发出现 AEB、牵引力控制、车身稳定控制及能量回收相关警报的问题。
- 修复网页手动重启时可能出现的同类偶发警报。
- 加强“CAN 写入关闭”状态下的被动监听保护，减少设备重启过程对车辆总线的影响。
- 优化 CAN 启动、停止和重新初始化时序，避免不必要的重复初始化。
- 提升 OTA 写入期间的 CAN 接收稳定性。
- 固件内部版本号、后台显示版本号统一更新为 `V1.0.2`。

## 升级方式

1. 将车辆停稳，并保持 `CAN 写入` 为关闭状态。
2. 在 WIFI-NAG 后台上传 `WIFI-NAG-V1.0.2-OTA.bin`。
3. 上传完成后等待设备自动重启，期间不要断电。
4. 重新连接 WIFI-NAG，确认固件版本显示为 `V1.0.2`。

## 说明

本版本不改变现有 Nag 模式、扭矩范围、WiFi 中继、DNS 过滤及 OTA 分区结构，仅针对重启和 OTA 稳定性进行修复。
