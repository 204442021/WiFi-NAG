# WIFI-NAG V1.0.3

本版本统一 V1.0.3 分支已合入功能的项目版本元数据，并修正回归测试对生成后 WebUI 页面文件的读取关系。

## 更新内容

- 保留 V1.0.3 已合入的 CAN 架构重建、BLE 联动与 WebUI 功能逻辑，不在本次版本修正中改动。
- 将项目内部版本号从 `V1.0.2` 更新为 `V1.0.3`，后台固件版本继续由构建时的项目版本动态显示。
- 明确 `include/web/mcp2515_dashboard_ui.h` 为稳定包装入口，真实生成页面位于 `include/web/mcp2515_dashboard_ui.base.h`。
- 修正固件信息与 Wi-Fi 设置回归测试，使包装关系和真实页面内既有 UI 控件分别接受验证。
- 保留既有 V1.0.2 发布说明、发布记录和发布工作流，不将其错误改写为当前版本内容。

## 升级说明

1. 使用版本总控在编译发布阶段确认的 V1.0.3 固件文件。
2. 在 WIFI-NAG 后台通过本地手动 OTA 上传固件。
3. 设备重启后，确认后台固件版本显示为 `V1.0.3`。

## 状态说明

- 本次版本修正不创建、修改或启用发布工作流。
- 本次提交不执行测试、编译或发布；对应结果由后续版本总控阶段确认。
