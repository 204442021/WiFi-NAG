# WIFI-NAG V1.0.6

本版本将障碍物换挡改为条件驱动的持续虚拟 P 注入，并将固件内部版本统一更新为 `V1.0.6`。

## 更新内容

- 修正 WIFI-NAG 对 T2CAN BrakeState 原因码的解释，按当前线协议识别制动有效、释放、超时、挡位与车速等状态。
- 仅当功能开启、T2CAN 物理刹车已踩下、T2CAN 车速新鲜且确认静止、T2CAN 挡位新鲜且为 D/R 时，才会持续随实时收到的有效原车 `0x118` 注入虚拟 P；松开刹车或任一安全条件失效立即停止。
- 删除手动虚拟 P 按钮和单次时间窗口：不缓存、不补发，也不按固定周期重放历史帧；每帧有效的真实 `0x118` 最多对应一次发送尝试。
- T2CAN 挡位为 P/N、挡位或车速未知/超过 500 ms、物理刹车释放或过期、CAN 不可写、OTA 或重启准备期间，均不会启动或维持虚拟 P 注入。
- 真实挡位与安全门只采用 T2CAN 状态；本地 `0x118` Byte2 不再作为 D/R 判断条件。

## 0x118 修改规则

- 只处理实时收到、校验有效的 8 字节标准 `0x118` 数据帧，不重放或补发历史帧。
- 保持原帧 Byte1、Byte3、Byte4、Byte5、Byte7 不变；Byte2 改为 `0x32`（P 挡），Byte6 改为 `0x00`。
- Byte0 按 Byte1..Byte7 之和再加 `0x19`，重新计算低 8 位校验。
- 原车 `0x118` 仍会存在；虚拟 P 只随安全条件持续成立期间新收到的有效原车帧发送。

## 安全说明

该功能仅供台架或封闭场地测试，不能作为道路安全功能使用。首次实车验证应准备随时断开设备，并观察 CAN 发送失败、T2CAN 数据新鲜度、车速和挡位状态。
