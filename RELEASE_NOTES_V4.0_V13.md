# V4.0 V13

本版本以 V2.0 代码为基础新增 `ADAPTIVE` NAG 模式，并保留原有 `A` 与 `A_V2` 模式。

## 自适应 NAG

- 仅处理 CAN ID `0x370 / 880`、DLC 至少为 8 且校验和有效的真实输入帧。
- 从原帧读取方向盘扭矩；正扭矩对应负向输出，负扭矩对应正向输出。
- 默认输出幅值 `1.80 Nm`、扭矩死区 `0.05 Nm`，死区内不发送。
- 角度安全门按绝对值判断：`|方向盘角度| >= 50.0°` 时停止发送。
- 角度回到 `|方向盘角度| <= 45.0°` 且连续保持 3 个有效帧后恢复。
- 启用、切换模式或修改设置后先监听 3 个有效帧再进入发送阶段。
- 默认发送 `10 s`，再随机只监听 `1 .. 3 s`，之后循环。
- 输出帧保留原始方向盘角度位，只修改扭矩、hands-on、counter 和 checksum。
- 自回声使用完整 8 字节帧匹配，且只有底层发送成功后才计入成功数与回声指纹。

## WebUI 与配置

- 增加 ADAPTIVE 模式按钮和独立设置区。
- 可配置输出扭矩、扭矩死区、绝对角度限制、发送时长、暂停最小/最大时长。
- 实时显示阶段、方向盘角度、真实/目标扭矩、倒计时、当前随机暂停值、阻断原因和诊断计数。
- 设置通过 `/api/nag-adaptive` 读写，并保存到 NVS。

## 默认值与限制

- 输出扭矩：`0.10 .. 1.80 Nm`
- 扭矩死区：`0.00 .. 0.50 Nm`
- 绝对角度门：`10.0 .. 180.0°`
- 发送时长：`1 .. 60 s`
- 暂停时长：`1 .. 30 s`；最小值大于最大值时自动交换
