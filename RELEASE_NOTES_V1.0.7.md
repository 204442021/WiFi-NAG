# WIFI-NAG V1.0.7

本版本仅回退 NAG 输入扭矩与 `0x370` 回声逻辑，并将固件内部版本统一更新为 `V1.0.7`。BLE、障碍物控制器、CAN 过滤器与底层架构保持不变。

## NAG 恢复内容

- 恢复 V1.0.2 的逐帧回声行为：CAN 写入开启时，每个真实 `0x370` 帧都会及时生成一次基于原帧的 NAG 回声。
- 模式 A 保持固定 `+1.80 Nm`。
- 模式 A_V2 恢复在 `+1.50 .. +1.80 Nm` 范围内按 `2000 ms` 周期改变输入扭矩；真实 `0x370` 回声仍逐帧发送，扭矩变化周期不再充当 CAN 发送间隔。
- 删除已废弃的 NAG 发送等待层、后台接口和页面设置，避免真实帧在等待期间被跳过。

## 功能边界

- 障碍物换挡开关强制关闭；启用请求会被忽略并保持为关闭状态。
- 不修改 `0x118` 障碍物控制器、BLE 联动实现、CAN 过滤器、TWAI 驱动或应用架构。
- NAG 仍只处理 DLC 为 8 的真实 `0x370 / 880` 帧，并继续使用原有 counter、checksum 与自回声抑制规则。

## 安全说明

本固件仅供开源学习、研究和封闭场地测试。CAN 写入会影响车辆行为；首次验证应先保持 CAN Write 关闭，确认接收与状态正常后再谨慎启用，并准备随时断开设备。
