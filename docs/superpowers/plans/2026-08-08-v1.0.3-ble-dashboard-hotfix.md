# V1.0.3 BLE Dashboard Hotfix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 BLE 卡片插入失败导致整个 WiFi-NAG 后台空白的问题，并重新验证、编译和覆盖发布现有 `V1.0.3` 固件。

**Architecture:** 保留现有 `/ → /legacy-dashboard → /dashboard` 三层兼容路由，只修改 BLE 页面组装器。BLE 插入位置改为按唯一元素 ID 的宽松正则定位；定位失败时原样写回 `/dashboard` HTML，BLE 失败不再阻断 NAG、Wi-Fi、系统状态和 OTA。新增读取真实生成页面的 Python 回归测试，并沿用现有 V1.0.3 全量测试与发布门禁。

**Tech Stack:** ESP-IDF、PlatformIO、C++ 嵌入式原始字符串、浏览器 JavaScript、Python `unittest`、GitHub Actions。

## Global Constraints

- 仓库：`204442021/WiFi-NAG`。
- 基线分支：`V1.0.3`。
- 基线提交：`06c66b0083265397342ffd44bde0f0015f9f7b7b`。
- 唯一工作分支：`V1.0.3-BLE后台热修复`。
- 所有源码修改和提交必须直接发生在 GitHub 工作分支，禁止本地提交。
- 版本号保持 `V1.0.3`，不得创建 `V1.0.4`。
- 不取消现有外层 iframe、BLE 中间组装页或 `/dashboard` 原后台路由。
- 不修改 BLE 扫描、配对、绑定、状态同步、障碍物转发、CAN 快速路径、Wi-Fi、DNS、OTA 或系统状态后端逻辑。
- BLE 插入失败必须显示原后台；只有 `/dashboard` 请求失败时才显示页面加载失败。
- 最终发布资产必须由修复后的 `V1.0.3` 正式源码 SHA 重新构建，覆盖同名 GitHub Release 资产。

---

### Task 1: 添加 BLE 页面组合失败回归测试

**Files:**
- Create: `test/test_ble_dashboard_composition_regression.py`
- Read: `include/ble/bridge_webui.h`
- Read: `include/web/mcp2515_dashboard_ui.base.h`

**Interfaces:**
- Consumes: `BLE_BRIDGE_SHELL` 中的 JavaScript 页面组合逻辑和真实生成后的 dashboard HTML。
- Produces: 回归契约，要求插入定位兼容带引号、单引号、无引号和属性顺序变化，并要求找不到插入点时继续执行 `document.write(html)`。

- [ ] **Step 1: 写入当前实现必然失败的回归测试**

创建 `test/test_ble_dashboard_composition_regression.py`：

```python
from __future__ import annotations

import re
import unittest
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
BLE_WEBUI_FILE = ROOT / "include" / "ble" / "bridge_webui.h"
UI_BASE_FILE = ROOT / "include" / "web" / "mcp2515_dashboard_ui.base.h"

HOTSPOT_TAG = re.compile(
    r"<div\b[^>]*\bid\s*=\s*(?:\"wifi-hotspot-section\"|'wifi-hotspot-section'|wifi-hotspot-section)(?=[\s>])[^>]*>",
    re.IGNORECASE,
)


class BleDashboardCompositionRegressionTests(unittest.TestCase):
    @classmethod
    def setUpClass(cls) -> None:
        cls.ble_webui = BLE_WEBUI_FILE.read_text(encoding="utf-8")
        cls.ui_base = UI_BASE_FILE.read_text(encoding="utf-8-sig")

    def test_real_generated_dashboard_uses_supported_hotspot_id_form(self) -> None:
        match = HOTSPOT_TAG.search(self.ui_base)
        self.assertIsNotNone(match)
        self.assertIn("id=wifi-hotspot-section", match.group(0))

    def test_locator_accepts_quoted_unquoted_and_reordered_attributes(self) -> None:
        variants = (
            '<div class="subsec" id="wifi-hotspot-section" data-x="1">',
            "<div class='subsec' id='wifi-hotspot-section' data-x='1'>",
            "<div class=subsec id=wifi-hotspot-section data-x=1>",
            "<div data-x=1 id=wifi-hotspot-section class=subsec>",
        )
        for html in variants:
            with self.subTest(html=html):
                self.assertIsNotNone(HOTSPOT_TAG.search(html))

    def test_ble_shell_uses_attribute_tolerant_hotspot_locator(self) -> None:
        self.assertIn("const hotspotTag=/<div\\b[^>]*\\bid\\s*=", self.ble_webui)
        self.assertIn("const hotspot=hotspotTag.exec(html);", self.ble_webui)
        self.assertIn("html.slice(0,hotspot.index)+card+html.slice(hotspot.index)", self.ble_webui)

    def test_missing_ble_insertion_point_fails_open_to_original_dashboard(self) -> None:
        self.assertNotIn(
            "throw new Error('BLE card insertion point missing')",
            self.ble_webui,
        )
        self.assertIn(
            "console.warn('BLE card insertion point missing; showing dashboard without BLE card')",
            self.ble_webui,
        )
        warning_index = self.ble_webui.index(
            "console.warn('BLE card insertion point missing; showing dashboard without BLE card')"
        )
        write_index = self.ble_webui.index("document.open();document.write(html);document.close();")
        self.assertLess(warning_index, write_index)

    def test_ble_card_remains_before_wifi_hotspot_when_locator_matches(self) -> None:
        hotspot = HOTSPOT_TAG.search(self.ui_base)
        self.assertIsNotNone(hotspot)
        composed = self.ui_base[: hotspot.start()] + '<div id="ble-bridge-section"></div>' + self.ui_base[hotspot.start() :]
        self.assertLess(composed.index('id="ble-bridge-section"'), composed.index("id=wifi-hotspot-section"))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 运行新增测试并确认当前代码失败**

Run:

```bash
python -m unittest test.test_ble_dashboard_composition_regression -v
```

Expected: 至少以下断言失败：

```text
test_ble_shell_uses_attribute_tolerant_hotspot_locator
test_missing_ble_insertion_point_fails_open_to_original_dashboard
```

失败原因应为当前代码仍使用精确字符串 `marker`，并抛出 `BLE card insertion point missing`。

- [ ] **Step 3: 提交失败测试**

```bash
git add test/test_ble_dashboard_composition_regression.py
git commit -m "test: cover BLE dashboard fail-open composition"
```

---

### Task 2: 实现属性宽松定位和失败放行

**Files:**
- Modify: `include/ble/bridge_webui.h`，仅修改 `BLE_BRIDGE_SHELL` 内的页面组合 JavaScript。
- Test: `test/test_ble_dashboard_composition_regression.py`

**Interfaces:**
- Consumes: `/dashboard` 返回的完整 HTML 字符串 `html` 和现有 BLE 卡片字符串 `card`。
- Produces: `hotspotTag: RegExp`、`hotspot: RegExpExecArray | null`；匹配时将 BLE 卡片插在 Wi-Fi 热点卡片前，不匹配时原样输出 dashboard。

- [ ] **Step 1: 用正则定位替换精确 marker**

在 `BLE_BRIDGE_SHELL` 中，将：

```javascript
const marker='<div class="subsec" id="wifi-hotspot-section"';
```

替换为：

```javascript
const hotspotTag=/<div\b[^>]*\bid\s*=\s*(?:"wifi-hotspot-section"|'wifi-hotspot-section'|wifi-hotspot-section)(?=[\s>])[^>]*>/i;
```

该表达式必须以唯一 ID 为锚点，不依赖 `class`、属性顺序或属性引号。

- [ ] **Step 2: 将阻断式异常改为失败放行**

将：

```javascript
if(!html.includes(marker))throw new Error('BLE card insertion point missing');
html=html.replace(marker,card+marker);
document.open();document.write(html);document.close();
```

替换为：

```javascript
const hotspot=hotspotTag.exec(html);
if(hotspot){
  html=html.slice(0,hotspot.index)+card+html.slice(hotspot.index);
}else{
  console.warn('BLE card insertion point missing; showing dashboard without BLE card');
}
document.open();document.write(html);document.close();
```

不得把 `document.write(html)` 放入 `if(hotspot)` 分支；否则找不到 BLE 插入点时仍会出现空白后台。

- [ ] **Step 3: 运行新增回归测试并确认通过**

Run:

```bash
python -m unittest test.test_ble_dashboard_composition_regression -v
```

Expected: `Ran 5 tests ... OK`。

- [ ] **Step 4: 运行原有 WebUI 回归测试**

Run:

```bash
python -m unittest \
  test.test_firmware_info_regression \
  test.test_wifi_settings_regression -v
```

Expected: 全部通过，证明没有删除或降低原后台固件信息与 Wi-Fi 控件断言。

- [ ] **Step 5: 提交最小修复**

```bash
git add include/ble/bridge_webui.h
git commit -m "fix: fail open when BLE dashboard insertion is unavailable"
```

---

### Task 3: 执行 V1.0.3 完整验证门禁

**Files:**
- Verify only: `test/`
- Verify only: `platformio.ini`
- Verify only: ESP32-S3 正式构建环境 `wifi_nag_ESP32_S3_CAN`

**Interfaces:**
- Consumes: Task 1 和 Task 2 的工作分支提交。
- Produces: 可审计的回归测试、原生测试和正式固件编译结果。

- [ ] **Step 1: 验证 Python 文件语法和全部回归测试**

Run:

```bash
python -m compileall -q scripts test
python -m unittest discover -s test -p "test_*regression.py" -v
```

Expected: 所有回归测试通过，其中包含新增 BLE 页面组合测试。

- [ ] **Step 2: 运行全部原生测试环境**

Run:

```bash
pio test -e native_nag
pio test -e native_nag_sweep
pio test -e native_twai
pio test -e native_log_buffer
pio test -e native_ble
```

Expected: 五个环境全部通过，无失败测试。

- [ ] **Step 3: 编译 ESP32-S3 正式环境**

Run:

```bash
pio run -e wifi_nag_ESP32_S3_CAN
```

Expected: `SUCCESS`，并生成非空 `.pio/build/wifi_nag_ESP32_S3_CAN/firmware.bin`。

- [ ] **Step 4: 检查工作分支修改范围**

Run:

```bash
git diff --check 06c66b0083265397342ffd44bde0f0015f9f7b7b..HEAD
git diff --name-only 06c66b0083265397342ffd44bde0f0015f9f7b7b..HEAD
```

Expected: 仅包含设计/计划文档、新增回归测试和 `include/ble/bridge_webui.h`；不得出现 CAN、Wi-Fi、DNS、OTA 后端文件的意外修改。

- [ ] **Step 5: 记录验证结果提交**

如项目采用验证记录文件，则创建 `docs/testing/V1.0.3-BLE-dashboard-hotfix-results.md`，写入各命令、结果和构建产物路径后提交：

```bash
git add docs/testing/V1.0.3-BLE-dashboard-hotfix-results.md
git commit -m "test: record V1.0.3 BLE dashboard hotfix verification"
```

如果仓库不保留测试结果文件，则不创建无必要文档，直接进入合入步骤。

---

### Task 4: 合入正式分支并覆盖发布 V1.0.3

**Files:**
- Modify on release branch: `.github/workflows/release-v1.0.3.yml`
- Existing: `scripts/release_v1_0_3_images.py`
- Release assets: `dist/WIFI-NAG-V1.0.3-*`

**Interfaces:**
- Consumes: 已通过全部验证的热修工作分支 HEAD。
- Produces: 修复后的 `V1.0.3` 正式源码提交、更新后的 `V1.0.3` tag 和覆盖后的 GitHub Release 同名资产。

- [ ] **Step 1: 将热修提交合入 `V1.0.3`**

合入前核对：

```bash
git merge-base V1.0.3 V1.0.3-BLE后台热修复
git diff --name-status V1.0.3...V1.0.3-BLE后台热修复
```

Expected: merge base 为 `06c66b0083265397342ffd44bde0f0015f9f7b7b`，修改范围符合 Task 3。

将已验证提交合入 `V1.0.3`，记录新的正式源码 SHA 为 `<FIXED_SOURCE_SHA>`。

- [ ] **Step 2: 更新发布工作流的精确源码目标**

在独立发布分支中，把 `.github/workflows/release-v1.0.3.yml` 的：

```yaml
RELEASE_TARGET: 06c66b0083265397342ffd44bde0f0015f9f7b7b
```

替换为：

```yaml
RELEASE_TARGET: <FIXED_SOURCE_SHA>
```

工作流仍必须校验 `origin/V1.0.3` 等于 `<FIXED_SOURCE_SHA>`，并运行 Task 3 的完整测试、编译、镜像生成和校验流程。

- [ ] **Step 3: 触发正式覆盖发布**

发布流程必须重新生成并以 `--clobber` 覆盖：

```text
WIFI-NAG-V1.0.3-OTA.bin
WIFI-NAG-V1.0.3-FACTORY-16MB.bin
WIFI-NAG-V1.0.3-MANIFEST.json
WIFI-NAG-V1.0.3-SHA256SUMS.txt
WIFI-NAG-V1.0.3-FLASH-GUIDE.txt
```

不得复用旧构建产物。

- [ ] **Step 4: 更新并验证 `V1.0.3` tag**

将 `V1.0.3` tag 指向 `<FIXED_SOURCE_SHA>`，然后验证：

```bash
git fetch --force origin refs/tags/V1.0.3:refs/tags/V1.0.3
test "$(git rev-list -n 1 V1.0.3)" = "<FIXED_SOURCE_SHA>"
```

- [ ] **Step 5: 校验发布资产来源和完整性**

检查 Release manifest 中：

```json
{
  "version": "V1.0.3",
  "source_sha": "<FIXED_SOURCE_SHA>",
  "build_environment": "wifi_nag_ESP32_S3_CAN"
}
```

并执行 SHA256 校验及：

```bash
python -m esptool --chip esp32s3 image_info WIFI-NAG-V1.0.3-OTA.bin
```

Expected: 所有资产存在、校验通过、OTA 镜像可识别、16MB factory 镜像大小为 `16777216` 字节。

- [ ] **Step 6: 输出最终报告**

报告必须包含：

```text
工作分支及修复提交 SHA
V1.0.3 正式源码 SHA
正式源码父提交 SHA
修改文件
完整测试结果
编译结果
发布工作流运行编号和结论
V1.0.3 tag 指向
Release 资产及 SHA256
已知风险
```
